# 老街品牌局开发规则

## 开发顺序

严格按照 `开发计划-老街品牌局.md` 的 M0–M8 顺序执行。每个里程碑先补测试、运行测试并通过验收门，再进入下一阶段。

## 绝对不变量

- 服务端是正式结算来源，客户端只能提交动作，不能提交结果或指标。
- 生产班级绑定不可变内容版本和班级种子；教师后台没有内容编辑接口。
- 首局 `first_run` 不可覆盖；重玩使用 `replay`，不得污染首局分析。
- 决策、查看证据、预测、视觉测试、事件、快照和报告必须可审计、可重放。
- 规则引擎不得访问网络、数据库、系统时间或 `Math.random()`。
- 事实、机制、解释分层；报告中的理论结论必须回指本局证据。
- 姓名和学号仅用于教师授权视图，不能进入公开页面、匿名投影、URL 或普通日志。

## 提交规则

- 每次改动只对应一个任务 ID；不得删除或跳过测试。
- Schema、规则或内容改动必须同时更新测试和对应文档。
- 迁移不可覆盖生产历史数据；需要修正时新增迁移或补偿记录。
- 运行 `pnpm lint`, `pnpm typecheck`, `pnpm test` 后才能提交。
