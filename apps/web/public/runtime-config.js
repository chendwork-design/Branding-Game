// The production static server replaces this with the API addresses for its
// current environment. Keeping an empty default makes local Vite development
// fall back to the same-host defaults without a failed script request.
window.__LAOJIE_RUNTIME_CONFIG__ = window.__LAOJIE_RUNTIME_CONFIG__ || {};
